(() => {
  'use strict';
  const app=document.querySelector('#app');
  const route=()=>location.hash.replace(/^#/,'').split('?')[0]||'home';
  const AREA_KEY='titans:v14MediaArea';
  let data=null,loading=null,timer=0;
  const esc=v=>String(v??'').replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[c]));
  const nextGame=()=>{const now=Date.now();return (data?.games||[]).find(g=>{const t=Date.parse(g.date);return Number.isFinite(t)&&t>now&&!/final|bye/i.test(String(g.status||''))})||null};
  async function load(){if(data)return data;if(loading)return loading;loading=fetch('/api/data',{cache:'no-store'}).then(r=>r.ok?r.json():null).catch(()=>null).then(d=>{data=d?.ok?d:null;return data}).finally(()=>loading=null);return loading}
  const deviceZone=()=>{try{return Intl.DateTimeFormat().resolvedOptions().timeZone||'UTC'}catch{return'UTC'}};
  function zoneParts(value,timeZone){const d=new Date(value);if(Number.isNaN(d.getTime()))return{date:'TBD',time:'TBD',code:''};const locale=navigator.language||'en-US';const date=new Intl.DateTimeFormat(locale,{weekday:'short',month:'short',day:'numeric',year:'numeric',timeZone}).format(d);const time=new Intl.DateTimeFormat(locale,{hour:'numeric',minute:'2-digit',timeZoneName:'short',timeZone}).format(d);const code=(time.match(/\b(?:GMT[+-]\d+|[A-Z]{2,5})\b/g)||[]).at(-1)||'';return{date,time,code}}
  function countdown(value){const then=Date.parse(value),diff=then-Date.now();if(!Number.isFinite(then))return'Time TBD';if(diff<=0)return'Game time';const mins=Math.floor(diff/60000),days=Math.floor(mins/1440),hours=Math.floor((mins%1440)/60),minutes=mins%60;if(days>0)return`${days}d ${hours}h`;if(hours>0)return`${hours}h ${minutes}m`;return`${Math.max(1,minutes)}m`}
  function watchLine(g,area){const n=String(g?.network||'').toUpperCase();if(/CBS/.test(n))return area==='nashville'?'CBS locally → Paramount+ or your local CBS station':'CBS game → Paramount+ if it is your local CBS game; otherwise check Sunday Ticket eligibility';if(/FOX/.test(n))return area==='nashville'?'FOX locally → FOX / FOX Sports with supported TV access':'FOX game → local FOX where carried; out-of-market Sunday afternoon games may require NFL Sunday Ticket';if(/NBC/.test(n))return'NBC → Peacock / NBC';if(/ESPN|ABC/.test(n))return'ESPN / ABC → ESPN watch options';if(/PRIME|AMAZON|TNF/.test(n))return'Prime Video → Thursday Night Football';if(/NFL NETWORK/.test(n))return'NFL Network → NFL Network / NFL+ according to your plan and device';if(/NETFLIX/.test(n))return'Netflix → open Netflix for this NFL game';return`${g?.network||'Network TBD'} → open the authorized provider choices below`}
  function listenLine(area){return area==='nashville'?'Nashville radio → WGFX 104.5 FM The Zone; digital playback may have game/geo restrictions':'All-game audio → NFL+; Titans call also available through licensed services such as TuneIn Premium and SiriusXM where supported'}
  function gameName(g){if(!g)return'Titans game';return `${g.homeAway==='home'?'Titans vs':'Titans at'} ${g.opponent||g.opponentAbbr||'Opponent'}`}
  function timeRow(label,sub,value,primary=false){return`<div class="media-time-row ${primary?'primary':''}"><div><small>${esc(label)}</small><span>${esc(sub)}</span></div><strong>${esc(value.time)}</strong><em>${esc(value.date)}</em></div>`}
  function renderGuide(){if(route()!=='media')return;const watch=document.querySelector('.media-watch');if(!watch||watch.querySelector('.media-tune-guide'))return;const g=nextGame(),area=localStorage.getItem(AREA_KEY)||'nashville';if(!g)return;const localZone=deviceZone(),local=zoneParts(g.date,localZone),et=zoneParts(g.date,'America/New_York'),ct=zoneParts(g.date,'America/Chicago'),utc=zoneParts(g.date,'UTC');const guide=document.createElement('section');guide.className='media-tune-guide';guide.innerHTML=`<header><div><small>NEXT TITANS BROADCAST</small><h3>${esc(gameName(g))}</h3><p>Everything you need to tune in, with kickoff converted automatically.</p></div><div class="media-countdown"><small>STARTS IN</small><strong data-media-countdown>${esc(countdown(g.date))}</strong></div></header><div class="media-time-grid">${timeRow('YOUR TIME',localZone,local,true)}${timeRow('EASTERN TIME','ET · New York',et)}${timeRow('NASHVILLE TIME','CT · Tennessee',ct)}${timeRow('WORLD TIME','UTC',utc)}</div><div class="media-tune-simple"><div><small>WATCH</small><strong>${esc(g.network||'Network TBD')}</strong><span>${esc(watchLine(g,area))}</span></div><div><small>LISTEN</small><strong>${area==='nashville'?'104.5 FM · WGFX':'Titans live audio'}</strong><span>${esc(listenLine(area))}</span></div></div><p class="media-time-help">Times use your device timezone automatically. ET/CT labels stay familiar while the displayed clock shows the correct daylight code (EDT/CDT) or standard code (EST/CST) for the game date.</p>`;watch.querySelector('header')?.insertAdjacentElement('afterend',guide);clearInterval(timer);timer=setInterval(()=>{const el=document.querySelector('[data-media-countdown]');if(el)el.textContent=countdown(g.date)},30000)}
  async function run(){await load();renderGuide()}
  window.addEventListener('hashchange',()=>setTimeout(run,50));
  window.addEventListener('storage',event=>{if(event.key===AREA_KEY&&route()==='media'){document.querySelector('.media-tune-guide')?.remove();run()}});
  if(app)new MutationObserver(()=>queueMicrotask(renderGuide)).observe(app,{childList:true,subtree:true});
  setTimeout(run,120);
})();
