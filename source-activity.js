const saQs=(s,r=document)=>r.querySelector(s),saRoute=()=>location.hash.replace(/^#/,'').split('?')[0]||'home';
let saPromise=null;
const saEsc=v=>String(v??'').replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[c]));
const saDate=v=>{if(!v)return 'Unknown';const d=new Date(v);return Number.isNaN(d.getTime())?String(v):new Intl.DateTimeFormat('en-US',{month:'short',day:'numeric',hour:'numeric',minute:'2-digit',timeZone:'America/Chicago'}).format(d)};
const statusLabel=run=>run.status==='success'?'Verified':run.status==='skipped'?'Skipped':'Failed';
async function loadActivity(){if(saPromise)return saPromise;saPromise=fetch('/api/data',{headers:{Accept:'application/json'}}).then(r=>r.json()).then(d=>d?.ok?d:null).finally(()=>{saPromise=null});return saPromise;}
function activityCard(run){const status=String(run.status||'unknown').toLowerCase(),seen=Number(run.recordsSeen||0),written=Number(run.recordsWritten||0),note=run.metadata?.note||'';return `<article class="source-activity-row ${saEsc(status)}"><div class="activity-status"><i></i><span>${saEsc(statusLabel(run))}</span></div><div class="activity-main"><strong>${saEsc(run.source||run.sourceSlug||'Source')}</strong><small>${saEsc(run.job||'source-check')} · ${saEsc(saDate(run.startedAt))}</small>${note?`<p>${saEsc(note)}</p>`:''}</div><div class="activity-counts"><span><b>${seen}</b> seen</span><span><b>${written}</b> written</span></div></article>`;}
async function applySourceActivity(){if(saRoute()!=='sources')return;const app=saQs('#app');if(!app||saQs('.source-activity-panel',app))return;try{const data=await loadActivity();if(!data)return;const runs=(data.syncRuns||[]).slice(0,10),latestVerified=runs.find(r=>r.status==='success'),panel=document.createElement('section');panel.className='source-activity-panel';panel.innerHTML=`<div class="source-activity-head"><div><small>Audit trail</small><h2>Source activity</h2><p>Health checks and imports are separate. “Written 0” means the source was checked without pretending data was imported.</p></div><div class="last-verified"><small>Last successful check</small><strong>${latestVerified?saEsc(saDate(latestVerified.startedAt)):'No successful check stored'}</strong></div></div>${runs.length?`<div class="source-activity-list">${runs.map(activityCard).join('')}</div>`:'<div class="team-room-empty">No source-check history stored yet.</div>'}`;const anchor=saQs('.source-arbitration',app)||saQs('.source-quality-banner',app)||saQs('.page-head',app);anchor?.insertAdjacentElement('afterend',panel);}catch{}}
const saApp=saQs('#app');if(saApp)new MutationObserver(()=>queueMicrotask(applySourceActivity)).observe(saApp,{childList:true,subtree:true});addEventListener('hashchange',()=>queueMicrotask(applySourceActivity));document.addEventListener('visibilitychange',()=>{if(document.visibilityState==='visible')queueMicrotask(applySourceActivity)});queueMicrotask(applySourceActivity);
