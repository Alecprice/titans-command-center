import {formatAmerican,americanToImplied} from './src/odds.mjs';
const mhEsc=v=>String(v??'').replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[c]));
const mhRoute=()=>location.hash.replace(/^#/,'').split('?')[0]||'home';
const dt=v=>v?new Intl.DateTimeFormat('en-US',{month:'short',day:'numeric',hour:'numeric',minute:'2-digit',timeZone:'America/Chicago'}).format(new Date(v)):'TBD';
function row(r){const implied=americanToImplied(r.price),ref=r.reference?'<em class="mh-ref">REFERENCE</em>':'';return `<div class="mh-row"><div><strong>${mhEsc(r.eventName||r.marketName||'Titans market')} ${ref}</strong><small>${mhEsc(r.marketName||'')} · ${mhEsc(r.book||r.provider||'')}</small></div><div><small>Side</small><b>${mhEsc(r.side||'—')}</b></div><div><small>Line</small><b>${r.line??'—'}</b></div><div><small>Price</small><b>${r.price==null?'—':formatAmerican(r.price)}</b></div><div><small>Implied</small><b>${implied==null?'—':(implied*100).toFixed(1)+'%'}</b></div></div>`}
function links(sources){return (sources||[]).map(s=>`<a href="${mhEsc(s.url)}" target="_blank" rel="noopener noreferrer">${mhEsc(s.label)} ↗</a>`).join('')}
async function loadMarket(force=false){if(mhRoute()!=='markets')return;const app=document.querySelector('#app');if(!app||app.dataset.marketHub==='loading'||(!force&&app.querySelector('.market-hub')))return;app.dataset.marketHub='loading';try{const r=await fetch('/api/market-data',{cache:force?'reload':'default'}),d=await r.json();if(!r.ok||!d.ok)throw new Error(d.error||'Market source unavailable');const head=app.querySelector('.page-head');app.innerHTML='';if(head)app.append(head);const hub=document.createElement('section');hub.className='market-hub';const isRef=d.sourceMode==='reference-snapshot',title=isRef?'Titans market reference board':'Live Titans market board';hub.innerHTML=`<div class="mh-head"><div><small>Odds & props</small><h2>${title}</h2><p>${mhEsc(d.message||'Market data refresh completed.')}</p></div><button id="mh-refresh" class="button primary">Refresh odds</button></div><div class="mh-status"><span><b>${mhEsc(d.provider||'No source')}</b> source</span><span><b>${(d.odds||[]).length}</b> market rows</span><span><b>${d.propsAvailable?'Available':'Unavailable without props feed'}</b> player props</span><span><b>${mhEsc(dt(d.fetchedAt))}</b> refreshed</span></div>${(d.events||[]).length?`<div class="mh-events">${d.events.map(e=>`<span>${mhEsc(e.name)} · ${mhEsc(e.status||dt(e.date))}</span>`).join('')}</div>`:''}${d.referenceNotice?`<div class="mh-reference-notice"><strong>Reference snapshot — not live odds.</strong><span>${mhEsc(d.referenceNotice)}</span>${links(d.sources)}</div>`:''}<div class="mh-table">${(d.odds||[]).map(row).join('')||'<div class="mh-empty">No market rows are available yet. Refresh will retry configured and keyless providers.</div>'}</div><div class="mh-note">Provider order: configured PropLine/Odds-API.io → keyless TheSpread.io → clearly labeled public reference snapshot. Player props are never invented when a source does not provide them. This section is informational only.</div>`;app.append(hub);hub.querySelector('#mh-refresh')?.addEventListener('click',()=>loadMarket(true));}catch(e){app.insertAdjacentHTML('beforeend',`<div class="mh-empty">Market refresh failed temporarily: ${mhEsc(e.message)}</div>`)}finally{delete app.dataset.marketHub}}
const mhRoot=document.querySelector('#app');if(mhRoot)new MutationObserver(()=>queueMicrotask(()=>loadMarket(false))).observe(mhRoot,{childList:true,subtree:true});addEventListener('hashchange',()=>queueMicrotask(()=>loadMarket(false)));queueMicrotask(()=>loadMarket(false));
